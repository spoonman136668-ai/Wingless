Plan a migration of a Go service from an in-memory job queue to a SQLite-backed durable queue. Constraints: no downtime, existing job IDs must remain stable, a job may never execute concurrently in two workers, crash recovery must be deterministic, retries must preserve attempt history, and rollback must be possible until the final cutover. Produce a staged plan with schema changes, compatibility period, invariants, acceptance tests, rollback points, and the exact gate that permits removal of the old queue.
