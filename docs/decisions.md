# Decisions

Machine-readable decisions are in decisions.json and mirrored into ICE. The Go standard library keeps offline tests reproducible. Narrow, fail-closed interfaces are preferred to pretending unfinished integration is operational. Source beats ICE. Deterministic fixture verification is separate from plane acceptance. Lifecycle is explicit but process management deferred. No shell authority is granted.
